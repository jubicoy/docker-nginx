server {
	listen       5000;
	server_name  localhost;
	root   /var/www;

	client_max_body_size 10G;

	location / {
		auth_basic "Restricted";
		auth_basic_user_file /tmp/auth;

		proxy_pass http://${BACKEND_URL};
	}
}
